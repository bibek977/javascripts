const vehicles = {
    car : "Tesla",
    bike : "Duke",
    train : "Bullet-train"
}

// to only gets values in an array
console.log(Object.values(vehicles))

// to get both values key and value in an array
console.log(Object.entries(vehicles))